name: Auto-Merge

# Auto-merge only ENABLES GitHub's native auto-merge (gh pr merge --auto) —
# the actual merge still waits on every required status check in branch
# protection (Detect Changes, Format & Lint, Test, Security Audit, Build x3,
# dependency-review). This workflow only decides *who* is trusted to skip
# manually clicking merge, not which checks must pass.
#
# Two independent trust paths, each separately gated:
#   - automerge-dependabot: dependabot[bot] PRs, gated on update type
#     (never auto-merges major version bumps) via dependabot/fetch-metadata.
#   - automerge-trusted: PRs opened by maintainers in TRUSTED_ACTORS below.
#     Triggered on `pull_request` (not `pull_request_target`), so GitHub
#     forces GITHUB_TOKEN to read-only for any fork-originated PR regardless
#     of the `permissions:` block here — a malicious fork PR cannot grant
#     itself merge rights by editing this file. The same-repo check below is
#     belt-and-suspenders on top of that platform guarantee.
#     release-plz Release PRs are EXCLUDED from this path (matched by the
#     `release-plz-` head-branch prefix and the `chore: release` title) so
#     cutting a release always requires an explicit human merge — the release
#     is the one action worth a deliberate gate, since it tags and publishes.
#
# To trust an additional maintainer, add their GitHub login to
# TRUSTED_ACTORS in the automerge-trusted job below.
#
# The merge step authenticates with a fine-grained PAT, not GITHUB_TOKEN.
# GitHub does not fire other workflows' `push`/`pull_request` triggers for
# events produced using the default GITHUB_TOKEN (loop-prevention) — so a
# merge performed with GITHUB_TOKEN silently skips CI, CodeQL, and release-plz
# on main, and no release PR ever gets opened for the merged change. Using a
# PAT here matches how release-plz.yml already authenticates for the same
# reason (see its comment / project CLAUDE.md).
#
# The two jobs use DIFFERENT PATs, by trust scope:
#   - automerge-trusted uses RELEASE_PLZ_TOKEN.
#   - automerge-dependabot uses DEPENDABOT_PAT (least-privilege: keeps the
#     broad release token out of the dependency-bump path).
# IMPORTANT: Dependabot-triggered runs can ONLY read *Dependabot* secrets, not
# Actions secrets. DEPENDABOT_PAT must therefore be added under Settings →
# Secrets and variables → *Dependabot* (adding it only to Actions leaves
# secrets.DEPENDABOT_PAT empty here and `gh pr merge` fails unauthenticated —
# see ENG-119). It needs Contents + Pull-requests: write.

on:
  pull_request:

permissions:
  contents: read

jobs:
  automerge-dependabot:
    name: Auto-Merge (Dependabot)
    runs-on: ubuntu-latest
    if: github.actor == 'dependabot[bot]'
    # GITHUB_TOKEN no longer performs the merge (see RELEASE_PLZ_TOKEN note
    # above) — read is all it needs for fetch-metadata.
    permissions:
      contents: read
    steps:
      - name: Harden the runner (Audit all outbound calls)
        uses: step-security/harden-runner@bf7454d06d71f1098171f2acdf0cd4708d7b5920 # v2.20.0
        with:
          egress-policy: audit

      - name: Fetch Dependabot metadata
        id: metadata
        uses: dependabot/fetch-metadata@25dd0e34f4fe68f24cc83900b1fe3fe149efef98 # v3.1.0
        with:
          github-token: "${{ secrets.GITHUB_TOKEN }}"

      - name: Auto-merge patch and minor updates
        if: steps.metadata.outputs.update-type != 'version-update:semver-major'
        run: gh pr merge --auto --squash "$PR_URL"
        env:
          PR_URL: ${{ github.event.pull_request.html_url }}
          # Dependabot secret (NOT the Actions secret) — see header note / ENG-119.
          GH_TOKEN: ${{ secrets.DEPENDABOT_PAT }}

  automerge-trusted:
    name: Auto-Merge (Trusted Maintainer)
    runs-on: ubuntu-latest
    # Same-repo check is redundant with GitHub's fork-PR token downgrade,
    # kept explicit so this condition is self-documenting and doesn't rely
    # on readers knowing the platform-level guarantee.
    if: |
      github.event.pull_request.head.repo.full_name == github.repository &&
      contains(fromJSON('["wingnut128"]'), github.actor) &&
      !startsWith(github.event.pull_request.head.ref, 'release-plz-') &&
      !startsWith(github.event.pull_request.title, 'chore: release')
    # GITHUB_TOKEN no longer performs the merge (see RELEASE_PLZ_TOKEN note
    # above) — this job needs no write access to the default token at all.
    permissions:
      contents: read
    steps:
      - name: Harden the runner (Audit all outbound calls)
        uses: step-security/harden-runner@bf7454d06d71f1098171f2acdf0cd4708d7b5920 # v2.20.0
        with:
          egress-policy: audit

      - name: Enable auto-merge
        run: gh pr merge --auto --squash "$PR_URL"
        env:
          PR_URL: ${{ github.event.pull_request.html_url }}
          GH_TOKEN: ${{ secrets.RELEASE_PLZ_TOKEN }}
